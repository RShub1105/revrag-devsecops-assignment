name: Secure Build and Deploy

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        run: echo "${{ secrets.DOCKER_HUB_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_HUB_USERNAME }}" --password-stdin

      - name: Build Docker image
        run: docker build -t ${{ secrets.DOCKER_HUB_USERNAME }}/myapp:${{ github.sha }} .

      - name: Scan image with Trivy
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ secrets.DOCKER_HUB_USERNAME }}/myapp:${{ github.sha }}
          format: 'table'
          exit-code: '0'
          severity: 'CRITICAL'
          ignore-unfixed: 'true'
          output: 'trivy-report.txt'

      - name: Upload scan report
        uses: actions/upload-artifact@v4
        with:
          name: trivy-report
          path: trivy-report.txt

      - name: Push image
        run: docker push ${{ secrets.DOCKER_HUB_USERNAME }}/myapp:${{ github.sha }}

      - name: Deploy
        run: |
          ssh deploy@${{ secrets.SERVER_IP }} \
          "docker pull ${{ secrets.DOCKER_HUB_USERNAME }}/myapp:${{ github.sha }} && docker run -d -p 80:3000 ${{ secrets.DOCKER_HUB_USERNAME }}/myapp:${{ github.sha }}"
